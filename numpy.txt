numpy
    array
        n-dimensional
        iterable
        consistant in type
        rectangular / non sparse
        immutable for shape, size, type (will create copies)
        mutable elements
        
ndarray = float64 by default
